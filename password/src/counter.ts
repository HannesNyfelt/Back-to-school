export function startCheck(element: HTMLInputElement) {
  //let strengthScore = 0;
  element.addEventListener('click', () => {
    console.log('test');

    //return char.toLowerCase() !== char.toUpperCase();
  })
}
